#define SIZE 10

#include <stdio.h>

int main() {
	int numbers[SIZE], i, j, counter = 0;

	for (i = 0; i < SIZE; i++) {
		scanf("%d", &numbers[i]);
	}

	for (i = 0; i < SIZE; i++) {
		for (j = 0; j < SIZE; j++) {
			// WIP
		}
	}

	return 0;
}
